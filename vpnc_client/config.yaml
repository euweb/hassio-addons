name: VPNC client add-on
version: "0.0.2"
slug: vpnc_client
description: VPNC client add-on
init: false
arch:
  - amd64
host_network: true
privileged:
  - NET_ADMIN
devices:
  - /dev/net/tun
options:
  debug: 0
  vpn-server:
    gateway:
    ID:
    secret:
    username:
    password:
  ping:
    server:
    wait: 60    
schema:
  debug: int
  vpn-server:
    gateway: str
    ID: str
    secret: password
    username: str
    password: password
  ping:
    server: str
    wait: int